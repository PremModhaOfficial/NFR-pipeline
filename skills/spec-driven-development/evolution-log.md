# Evolution Log — spec-driven-development

## 1.0.0 — bootstrap-seed — 2026-04-17
Initial wrapper with SDK re-scope: TPRD FRs replace user stories, symbol-traceability via `[traces-to:]` markers replaces cross-service story contracts.

- v1.1.0 (2026-05-06) — added "§OTel — Observability Contract authoring" section. Defines required TPRD fields, allowed enum values (`signals.*`, `consumer_provider_optin`, `log_correlation`, `nats_surface`, `tenant_attribution`), ambiguity rules (TBD/maybe/empty rejected). Aligns with G20 §OTel extension landing same revision.
